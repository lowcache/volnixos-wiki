# Hugo build output and caches.
/public/
/resources/
.hugo_build.lock

# Wrangler local state (build cache, dev server, deploy metadata).
/.wrangler/

# Direnv / editor scratch
/.direnv/
*.swp

# Impeccable design-workflow scratch (decision-page logs, generated mocks).
# PRODUCT.md and DESIGN.md are deliberately NOT ignored — they are the
# durable product and visual records.
/.impeccable/
