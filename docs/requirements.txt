# Documentation build dependencies for the MkDocs site.
# Local Nix builds can instead use:
#   nix shell nixpkgs#python313Packages.mkdocs-material -c mkdocs serve
#
# Pinned exactly, not floored. `mkdocs-material>=9.7` let every Cloudflare build
# resolve to whatever was newest that day: local builds used 9.7.6 while
# Cloudflare shipped 9.7.7, with different CSS/JS asset hashes. A theme release
# could then change the rendered wiki with no commit and no warning.
#
# These are the versions verified live on wiki.infernalcode.com (2026-08-05).
# mkdocs is pinned too — pinning only the theme leaves the generator floating.
#
# To upgrade: bump deliberately, rebuild, compare, then commit the new pin.
mkdocs-material==9.7.7
mkdocs==1.6.1
